{
  "name": "@af/sweph-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "husky": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}